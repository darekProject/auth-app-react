## App auth in React/Redux 

Client-side application. It enables setting up an account, logging in, logging out. It also has components that require authorization.

## Run app: 
- npm install
- npm start
